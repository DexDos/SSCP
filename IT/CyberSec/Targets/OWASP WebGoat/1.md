---
tags:
  - IT/CS/CTF
Machine: "[OWASP WebGoat](OWASP%20WebGoat.md)"
Topic: Broken Access Control
---
# tasks

## Hijack a session
Application developers who develop their own session IDs frequently forget to incorporate the complexity and randomness necessary for security. If the user specific session ID is not complex and random, then the application is highly susceptible to session-based brute force attacks.
### theory

### assignments
We are provided with a login form:
![Pasted image 20260801153804.png](../../../../Cache/IMGs/Pasted%20image%2020260801153804.png)

Let's perform a login attempt and see what will be the response:
![Pasted image 20260801171505.png](../../../../Cache/IMGs/Pasted%20image%2020260801171505.png)

Okay, so the request with valid credentials causes the server to send a `Set-Cookie` header with a kind of cookie named *"hijack_cookie"*. Let's send it to Repeater and try do discover more of those cookies in order to look for a pattern: 
![Pasted image 20260801171524.png](../../../../Cache/IMGs/Pasted%20image%2020260801171524.png)

We can send many requests to collect cookies: ...............Let's discover a new potentially valid *hijack-cookie*
![Pasted image 20260801172208.png](../../../../Cache/IMGs/Pasted%20image%2020260801172208.png)

And we can study if responses differentiate within other headers:
![Pasted image 20260801172118.png](../../../../Cache/IMGs/Pasted%20image%2020260801172118.png)

So that we have identified the only changing part of a valid request let's collect a few and see how are they getting generated:
![Pasted image 20260801172254.png](../../../../Cache/IMGs/Pasted%20image%2020260801172254.png)

So we definitely see that the cookie consists of two numeric parts. These parts do increase: by one and by some unknown amount respectively. It is worth considering that the second part would be a **UTC** time format (which is basically a number of seconds past from the 1st of January, 1972). 

So the point is, the server assigns a timestamp to the previous cookie's enormous number incremented by 1 (`map<integer:timestamp>`). Thus, the timestamp may only be valid if taken in between of the timestamps of the previous successful login and the next one. So that we just need to set the number to the value missing in our list and fuzz by the UTC values in the respective range:

| first                   | second range                  |
| ----------------------- | ----------------------------- |
| 48100847013848834**59** | 1785593758285 - 1785593758867 |
| 48100847013848834**61** | 1785593759466 - 1785593760094 |

Let's send the request to the **Intruder**, having the cookie added to *"Cookie"* header:
![Pasted image 20260801201315.png](../../../../Cache/IMGs/Pasted%20image%2020260801201315.png)

Since we are unaware whether the server saves only few last cookies for this assignment or not let's request a new cookie and use it:
![Pasted image 20260801180759.png](../../../../Cache/IMGs/Pasted%20image%2020260801180759.png)

So that we can now use the cookie found. It is important not to forget to increment the number by 1 (`4810084701384883488-1785595753282` $\rightarrow$ `4810084701384883489-1785595753282`). Then we need to set up fuzzing to sequential numeric fuzz with the UTC's last few digits. We may now start the attack:
![Pasted image 20260801180916.png](../../../../Cache/IMGs/Pasted%20image%2020260801180916.png)

So that we have right away received two responses with uncommon length:
![Pasted image 20260801175035.png](../../../../Cache/IMGs/Pasted%20image%2020260801175035.png)

Studying their content we can see congratulations for the assignment accomplishment:
![Pasted image 20260801175213.png](../../../../Cache/IMGs/Pasted%20image%2020260801175213.png)
## Insecure direct object references

### theory
Direct Object References are when an application uses client-provided input to access data & objects.
### assignments
The assignment simply wants us to login under the credentials specified (login: `tom`, password: `cat`):
![Pasted image 20260801182705.png](../../../../Cache/IMGs/Pasted%20image%2020260801182705.png)

The next stage is to observe our own profile (the Tom's one) data which is getting received by the client but is not shown on the page: 
![Pasted image 20260801182923.png](../../../../Cache/IMGs/Pasted%20image%2020260801182923.png)

As the assignment wants us to specify attributes, we can simply put them in:
![Pasted image 20260801183039.png](../../../../Cache/IMGs/Pasted%20image%2020260801183039.png)

The next assignment simply wants us to apply the logic and predict what would be the pattern of a RESTful API. 

>[!question]- REST
> **REST** (Representational State Transfer) — is a design pattern which is meant to guarantee a flexible and robust usability, predictable design and behavior. Unlike **gRPC** or **GraphQL** APIs, REST is a resource-oriented architecture which provides multiple endpoints for any resource might be needed (gRPC – instructions for other server's functions call; GraphQL – query-based technology). 
> ##### Key principles:
> - **CRUD** operations (create, read, update, delete)
> - Stateless server (no data about the session preserved in RAM)
> - Map resources on URLs
> - Key HTTP methods allowed:
> 	- GET
> 	- POST
> 	- PUT
> 	- PATCH
> 	- DELETE

We have also requested an `/WebGoat/IDOR/profile` endpoint in a previous assignment. Assuming that we may suppose that the server must have a `/WebGoat/IDOR/profile/${number}` endpoint which we should be able to request with `GET` method. Let's try this guess:
![Pasted image 20260801183324.png](../../../../Cache/IMGs/Pasted%20image%2020260801183324.png)

Great, let's move on.

The first part requires us to request data about another profile, The second one asks us to modify data about other users:
![Pasted image 20260801184135.png](../../../../Cache/IMGs/Pasted%20image%2020260801184135.png)

This means that this assignment wants us to bypass both horizontal (access) and vertical (authorization) security (PS, which is rather absent in this case). The importance of them both could vary from "somewhat considerable" to tremendous depending on a case. Although it is still worth acknowledging that some architectural principles may lead to some particular weaknesses which we do need to be handled and secured.

Let's fuzz through the profiles listed on a resource:
![Pasted image 20260801193250.png](../../../../Cache/IMGs/Pasted%20image%2020260801193250.png)

Among others there are few which inform us of successful accomplishment of the first part. There are also profile data listed in body within the *output* key value:
![Pasted image 20260801184118.png](../../../../Cache/IMGs/Pasted%20image%2020260801184118.png)

Let's send the request to the repeater. To achieve the goal we need to do the following:
- Change the method to PUT;
- Add a JSON-formatted profile data;
- Apply some modifications required:
	- Change color to "red"
	- Decrement the "role" key value
- Specify the `Content-Type: application/json` header;
So that we now may send the request:
![Pasted image 20260801194635.png](../../../../Cache/IMGs/Pasted%20image%2020260801194635.png)

It looks like we have done everything correctly, despite the role value. We are requested to downgrade the profile's role to an even lower value:
![Pasted image 20260801194652.png](../../../../Cache/IMGs/Pasted%20image%2020260801194652.png)

It looks like we have accomplished the assignment:
![Pasted image 20260801220057.png](../../../../Cache/IMGs/Pasted%20image%2020260801220057.png)
## Missing function level access control
Access control, like preventing XSS with output encoding, can be tricky to maintain. One must ensure it is adequately enforced throughout the entire application, thus in every method/function.

 **IDOR vs Missing Function Level Access Control**
The fact is many people (including the author of this lesson) would combine function level access control and IDOR into 'Access Control.' For the sake of OWASP Top 10 and these lessons, we will make a distinction. The distinction most made is that IDOR is more of a 'horizontal' or 'lateral' access control issue, and missing function level access control 'exposes functionality.' Even though the IDOR lesson here demonstrates how functionality may also be exposed (at least to another user in the same role), we will look at other ways functionality might be exposed.
### theory
### assignments
For the first assignment we are provided with some kind of UI with two dropdown lists `Account` and `Messages`. We need to inspect HTML and find two hidden elements:
![Pasted image 20260801220545.png](../../../../Cache/IMGs/Pasted%20image%2020260801220545.png)

Using *"select element"* tool we can conveniently auto-scroll listed HTML code down to the UI element needed:
![Pasted image 20260801220703.png](../../../../Cache/IMGs/Pasted%20image%2020260801220703.png)

Here we can see three unsorted list `<ul><li></li><li></li></ul>` elements, which so happen to be dropdowns. Two of them are seen by us on a webpage, but the third one is hidden:
![Pasted image 20260801220814.png](../../../../Cache/IMGs/Pasted%20image%2020260801220814.png)

Let's unfold the code nested and reveal hidden elements of and admin dropdown panel:
![Pasted image 20260801220926.png](../../../../Cache/IMGs/Pasted%20image%2020260801220926.png)

So that we have not only discovered two hidden elements required *Users* and *Config*, but we have also revealed endpoints `/Webgoat/access-control/users-admin-fix` and `/Webgoat/access-control/config`.

Let's fill out the assignment form and proceed to the next one:
![Pasted image 20260801221545.png](../../../../Cache/IMGs/Pasted%20image%2020260801221545.png)

The next assignment requires us to define the *Jerry*'s hash. Also, there is a statement that we will need to "do some guessing", which does not sound much promising:
![Pasted image 20260801231042.png](../../../../Cache/IMGs/Pasted%20image%2020260801231042.png)

Since we have uncovered hidden endpoints, lets try to use the endpoints revealed. To do so let's use interceptor to intercept the form submit and send it to repeater to modify requests: 
![Pasted image 20260802012240.png](../../../../Cache/IMGs/Pasted%20image%2020260802012240.png)

The second endpoint comes with the same output of `401` status code, which indicates we lack some valid credentials. Let's do some guessing based on the previously covered topics: there was a thematic task where we had to guess on endpoints. As we need a list of users to grab information about us let's simply guess the endpoint would be something like `/WebGoat/access-control/users`:
![Pasted image 20260801231026.png](../../../../Cache/IMGs/Pasted%20image%2020260801231026.png)

And that is indeed the case. Unfortunately, we have received the `415` status code, which means we are requesting some unsupported media type. Let's rebuild our request into GET method with another data format – JSON:
![Pasted image 20260801232549.png](../../../../Cache/IMGs/Pasted%20image%2020260801232549.png)

We have obtained the users list. Note that the *Jerry* user is admin, which might be helpful. Let's see if we have obtained the proper hash:
![Pasted image 20260801232754.png](../../../../Cache/IMGs/Pasted%20image%2020260801232754.png)

Great. The next assignment requires us to retrieve another kind of Jerry's hash, which is listed, supposedly, within `.../users-admin-fix` endpoint:
![Pasted image 20260801233121.png](../../../../Cache/IMGs/Pasted%20image%2020260801233121.png)

We can exploit the previously discovered hash for admin profile, which might be helpful. 
![Pasted image 20260801233106.png](../../../../Cache/IMGs/Pasted%20image%2020260801233106.png)

For now we are still unable to access the `.../users-admin-fix` endpoint receiving `401`. Let's try to bypass this limitation by adding a user record with POST method:
![Pasted image 20260801234346.png](../../../../Cache/IMGs/Pasted%20image%2020260801234346.png)

Now that we have added an admin record with the login exactly matching our own, we may attempt access to the endpoint. But firstly let's check if we were indeed added to the list:
![Pasted image 20260801234455.png](../../../../Cache/IMGs/Pasted%20image%2020260801234455.png)

Okay, lets request the endpoint:
![Pasted image 20260801234627.png](../../../../Cache/IMGs/Pasted%20image%2020260801234627.png)

So that we have obtained the second hash. Let's check if it is valid:
![Pasted image 20260801234655.png](../../../../Cache/IMGs/Pasted%20image%2020260801234655.png)

It looks like we have completed the topic.
## Spoofing an authentication cookie

**Concept**
Authentication cookies are used for services that require authentication. When a user logs in with a personal username and password, the server verifies the provided credentials. If they are valid, it creates a session.

Typically, each session is assigned a unique ID that identifies the user’s session. When the server sends a response back to the user, it includes a "Set-Cookie" header that contains, among other things, the cookie name and value.

The authentication cookie is usually stored on both the client and server sides.

On one hand, storing the cookie on the client side means it can be susceptible to theft through exploiting certain vulnerabilities or interception via man-in-the-middle attacks or XSS. On the other hand, the cookie values can be guessed if the algorithm used to generate the cookie is obtained.

Many applications will automatically log in a user if the correct authentication cookie is provided.

**Goals**
The user should not be able to guess the cookie generation algorithm and bypass the authentication mechanism by logging in as a different user.

**Rationale**
It is crucial for the security of the authentication system that the cookie generation algorithm remains secure and not easily guessable. If an attacker can predict or determine the algorithm, they may be able to generate valid authentication cookies for different users, thereby bypassing the authentication mechanism and impersonating other users.

To mitigate this risk, it is essential to employ robust and cryptographically secure algorithms for generating authentication cookies. These algorithms should use strong randomization and hashing techniques to ensure the uniqueness and unpredictability of the generated cookies.

Additionally, implementing measures such as session expiration and regular rotation of authentication cookies can further enhance security. By frequently changing the cookie values and enforcing session timeouts, the window of opportunity for attackers to exploit any potential vulnerabilities is significantly reduced.

Overall, protecting the confidentiality and integrity of the authentication cookie generation algorithm is paramount to prevent unauthorized access and maintain the integrity of the authentication mechanism.

### theory
### assignments
The assignment requires us to log in as *Tom*, providing us with two pairs of credentials:
![Pasted image 20260802022058.png](../../../../Cache/IMGs/Pasted%20image%2020260802022058.png)

Let's study the resources. Webgoat login attempt: 
![Pasted image 20260802022029.png](../../../../Cache/IMGs/Pasted%20image%2020260802022029.png)

Admin login attempt:
![Pasted image 20260802022149.png](../../../../Cache/IMGs/Pasted%20image%2020260802022149.png)

It seems the only information we have for an analysis is a `spoof_auth` cookie:

| login   | cookie                                           |
| ------- | ------------------------------------------------ |
| webgoat | NTQ0MTU3NjI2ZTZiNjQ0YzRlNmU3NDYxNmY2NzYyNjU3Nw== |
| admin   | NTQ0MTU3NjI2ZTZiNjQ0YzRlNmU2ZTY5NmQ2NDYx         |
Assuming that one of the cookies has `=` ending symbols, let's study if it could be `base64` encoded:
![Pasted image 20260802023008.png](../../../../Cache/IMGs/Pasted%20image%2020260802023008.png)

It has been a string, formatted as Hex and encoded into base64. Let's study the other cookie:
![Pasted image 20260802023108.png](../../../../Cache/IMGs/Pasted%20image%2020260802023108.png)

| login   | cookie                                           | initial value         |
| ------- | ------------------------------------------------ | --------------------- |
| webgoat | NTQ0MTU3NjI2ZTZiNjQ0YzRlNmU3NDYxNmY2NzYyNjU3Nw== | TAWbnkdLNn**taogbew** |
| admin   | NTQ0MTU3NjI2ZTZiNjQ0YzRlNmU2ZTY5NmQ2NDYx         | TAWbnkdLNn**nimda**   |
Interestingly, the initial value structure is really simple: the static string `TAWbnkdLNn` appended with a login, spelled the backwards. Let's recreate the cookie by appending the string with the `Tom` login, spelled backwards: `TAWbnkdLNnmoT`:
![Pasted image 20260802023447.png](../../../../Cache/IMGs/Pasted%20image%2020260802023447.png)

And let's try using this cookie to log in:
![Pasted image 20260802023429.png](../../../../Cache/IMGs/Pasted%20image%2020260802023429.png)

Well, as we have received congratulations let's see if we indeed passed the section:
![Pasted image 20260802023535.png](../../../../Cache/IMGs/Pasted%20image%2020260802023535.png)